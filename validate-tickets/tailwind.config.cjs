const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      colors: {
        ticketLighterBlue: "#7BC1CC",
        ticketLightBlue: "#5EB1BF",
        ticketDarkBlue: "#012A42",
      }
    },
  },

  plugins: [],
};

module.exports = config;
